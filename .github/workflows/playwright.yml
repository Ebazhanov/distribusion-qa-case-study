name: API Regression Suite

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: # Allows manual trigger from GitHub UI

# Grant write permissions so the default GITHUB_TOKEN can perform actions cleanly
permissions:
  contents: read
  checks: write

jobs:
  test:
    name: Run API Tests & Quality Checks
    timeout-minutes: 15
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check Formatting
        run: npm run format:check

      - name: Run Linter
        run: npm run lint

      - name: Run Playwright API Tests
        env:
          # If using a custom secret saved in GitHub Settings -> Secrets, use ${{ secrets.GH_TEST_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GH_TEST_TOKEN || secrets.GITHUB_TOKEN }}
        run: npm test

      - name: Upload Playwright HTML Report
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14